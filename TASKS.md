# Trout Tricks — Task Tracker

Persistent source of truth for active, paused, and closed work. All adds/closes/edits happen here.

## Open Loops — May 30, 2026

Authoritative current-state snapshot of all active work, grouped by priority. Detailed historical context for some items lives in the numbered tickets under **## Open (active queue)** and **## Backlog (paused, not active)** below; this section supersedes those for current status. Existing ticket IDs are cross-referenced where applicable.

### Priority 1 — Revenue / compliance blockers

- **1a — CDOR Revenue Online access** — Status: **BLOCKED**.
  - **Context:** Form submission rejected as "info wrong." Tried hyphen variants and ZIP+4. First call attempt to CDOR (☎️ 303-238-7378) hit a too-busy line. Account `97169111-0001` · FEIN `422215711` · Letter ID `L0463816592` · Liability date 2026-05-04.
  - **Acceptance:** Logged in to Revenue Online.
  - **Depends on:** —
- **1b — Confirm filing frequency** — Status: **BLOCKED on 1a**.
  - **Context:** License doesn't show frequency. Likely quarterly or annually given current revenue (~$200-300/mo gross).
  - **Acceptance:** Frequency documented here in TASKS.md.
  - **Depends on:** 1a.
- **1c — Cloudflare Worker tax collection** — Status: **TODO**.
  - **Context:** Square dashboard has CO Sales Tax enabled with automatic delivery/pickup overrides, but the Worker creates Hosted Checkout sessions via API and tax isn't auto-applying — all orders since 2026-05-04 show `tax_money: $0`. Fix candidates: include tax in line items, or set `pricing_options.auto_apply_taxes` on session creation. Estimated retroactive tax owed across May: **~$25-50** — document for first filing.
  - **Acceptance:** A test order through Hosted Checkout shows a tax line on the Square receipt.
  - **Depends on:** —
  - **Related ticket:** #53 in active queue (different angle on the same compliance umbrella).

### Priority 2 — List building (highest growth lever per TT12)

- **2 — Chironomid Cheat Sheet PDF lead magnet** — Status: **TODO** (identified in TT12 as "single best list-builder, ships in a week").
  - **Context:** 2-3 page PDF in Brian Chan-style chironomid presentation (depth zones, color by water clarity, retrieve speeds, four-pattern starter rotation). Gate via email signup at `trouttricks.com/cheat-sheet`. Klaviyo flow delivers the PDF + adds to Crew list (`SUpRkF`). Promotion: 1 FB community post/week for 4 weeks + r/flyfishing + CO Trout Unlimited chapter pages.
  - **Acceptance:** Landing page live, Klaviyo flow delivers PDF, **200 signups in 4-6 weeks**.
  - **Depends on:** —
  - **Related ticket:** #21-24 in backlog (previously gated on #13/#14 wholesale — decouple).

### Priority 3 — Relationship capital (manual, one-off)

- **3 — Personal post-purchase notes** — Status: **TODO**.
  - **Context:** Send each individually from `stillwaterflies5280@gmail.com` (NOT Klaviyo). Template lives in the TT13 chat transcript.
  - **Recipients:**
    - **David Romero** — 2026-05-27 · $54.50 · d111d@yahoo.com
    - **Ryan Greene** — 2026-05-27 · $54.50 · rynegreene8@gmail.com · Fort Collins, CO
    - **Trev Jasper** — 2026-05-27 · $42 · trevorjasper44@gmail.com
    - **Timothy Spaulding** — 2026-05-29 · $114.50 · tgspaulding1234@gmail.com (biggest order to date)
  - **Acceptance:** All 4 sent.
  - **Depends on:** —

### Priority 4 — Data-gated experiments

- **4 — FB Group paid ad test** — Status: **GATED** (today is day 3 post-50%-off-launch; need 5-7 more days of clean post-promo baseline data first).
  - **Context:** Engagement objective, **$50 budget, 7 days**. Targeting: Fly Fishing + (Lakes OR Stillwater), CO/WY/MT/NM/UT, ages 25-65. Creative: bench shot or lake report. CTA: "Join the community" → group link.
  - **Pre-flight:** Audit the group "shop window" (banner, pinned post, post quality) before launching ads.
  - **Acceptance:** Cost per group join measured; scale-or-kill decision made.
  - **Depends on:** 5-7 more days of clean post-promo baseline data.
- **5 — The Drop validation** — Status: **NEEDS AUDIT** (0 real waitlist signups in 30 days).
  - **Context:** Audit whether the Drop signup is visible anywhere customers actually see (site, emails, social). Distinguish a **distribution problem** (fix = promote it) from a **validation problem** (concept doesn't resonate). Hold Drop Welcome flow refresh until clear. Re-think date 2026-06-09 per TT13 backlog.
  - **Acceptance:** Audit done, root cause identified, decision made: promote-harder OR pivot/kill.
  - **Depends on:** —
  - **Related ticket:** #45 in backlog.

### Priority 5 — Technical polish (small wins, batch when energy permits)

- **6 — UTM tagging audit on all Klaviyo flows** — Status: **TODO**.
  - **Context:** Verify order confirmation, welcome, abandoned cart, post-purchase, and future campaign templates all carry `utm_source=klaviyo`, `utm_medium=email`, `utm_campaign={flow_name}`. Account default was set in TT12 but per-template verification needed. The Klaviyo / email line in GA4 should reflect this.
  - **Acceptance:** Every live flow template verified; GA4 attribution reflects tagged sends.
  - **Depends on:** —
- **7 — Sticker QR code tracking** — Status: **TODO** (~30 min).
  - **Context:** Thomas's parking-lot stickers are currently invisible to GA. Options: QR points to `trouttricks.com/?utm_source=sticker&utm_medium=guerrilla&utm_campaign=parking-lot`, OR a dedicated `/sticker` landing page with a free fly + `STICKER` code.
  - **Acceptance:** Sticker traffic shows up in GA4 under the chosen UTM.
  - **Depends on:** —
- **8 — AOV expansion lever** — Status: **TODO**.
  - **Context:** Test free shipping threshold over $40. Welcome popup could add a "free shipping over $40" badge.
  - **Acceptance:** Threshold live in cart math + messaging surfaced; track AOV delta vs baseline.
  - **Depends on:** —
- **9 — Cart structured address + acquisition_source** — Status: **READY** (release with next cart deploy).
  - **Context:** Wire cart to send structured address fields (`address_line1` / `city` / `state` / `zip`) and `acquisition_source` (e.g. `document.referrer`) so the new Code.gs columns auto-populate from real ship orders.
  - **Acceptance:** Next ship order populates the new columns automatically.
  - **Depends on:** Next cart deploy (release-bundle).
  - **Related ticket:** #54 in backlog.
- **10 — White stuffer cards** — Status: **TODO**.
  - **Context:** Each shipped box gets a card: "Share your catch in the FB group" + QR. Closes the loop: customer → group → social proof → more customers. Source: Uline pre-cut (~$40) OR DIY (~$20).
  - **Acceptance:** Initial batch (~200 cards) ordered + slipped into outgoing boxes.
  - **Depends on:** —
  - **Related ticket:** #72 in active queue.

### Priority 6 — Deferred (paused, revisit later)

- **#13** Avidmax wholesale (Nick contact)
- **#14** Umpqua signature pattern (blocked on Matt Winkler)
- **#15** Local fly shop wholesale (South Park / North Park / Estes / Carter Lake)
- **#20** UTM social bio links via Bit.ly + GA4
- **#45-48** Drop subscriptions / Apps Script / onboarding (gated on item 5 — Drop validation)
- **#52** 4-week Drop sprint (held)
- FB value-first cadence (Mon lake report / Wed tying reel / Fri photo) — start whenever
- Cross-promo DMs to 3-5 CO stillwater creators
- Clarity diagnostic (5-10 view-no-add sessions to identify funnel friction)
- GSC Performance → queries at position 5-20 with 50+ impressions (almost-ranking SEO wins)

### Insights worth preserving (from TT13 chat, May 27-30)

- **50% off promo + new welcome email + new order confirmation flow + Klaviyo UTM fixes deployed 2026-05-27.** Effects sustaining 3 days in: **AOV up from $24 → ~$66 (2.75×)**, **purchasers 12 in May vs 3 in April (+300%)**, **Google organic +218%** (compounding SEO), **Facebook share of traffic dropped 66% → 47.5%** (healthy diversification, not loss — FB absolute volume also grew). System is working as designed; **don't tinker for at least 2 more weeks.**
- **Klaviyo Placed Order events arrive via Square's native integration (NOT Apps Script).** Field structure: `Items` is a flat string array; structured money/discount/fulfillment data lives in `event.extra.*`. Order Confirmation template at `assets/email-templates/order-confirmation.html` rebuilt against this schema (commit `eca8a33` + later).
- **"Same shipping address, two orders" pattern observed** — David Romero + Ryan Greene both at 2203 Sandbur Dr, Fort Collins. Likely legitimate household co-purchase, not fraud. Verify via Square payment method comparison if the pattern repeats.

## Open (active queue)

### Next 7 days

- **#53** ⏰ **Colorado Sales Tax Compliance** — TIME-SENSITIVE. April reporting-period deadline (20th of month following) is already past. File at **Colorado.gov/RevenueOnline**. Letter ID **L046381652** · FEIN **422215711** · CO Account **971691110001**.
  - **53.0** ☎️ **Call CDOR Monday morning at 303-238-7378** — triage filing frequency (monthly vs quarterly vs annual based on volume) and confirm what's already been filed for April. Do this FIRST before any TaxJar setup; filing frequency determines whether 53.3's cadence is right.
  - **53.1** Sign up for TaxJar (free tier) — connect Square account.
  - **53.2** Set Colorado as filing location in TaxJar.
  - **53.3** Calendar reminder: **15th of each month** — check TaxJar totals, then file at Colorado.gov/RevenueOnline (5-day buffer before the 20th deadline). Adjust cadence if CDOR puts you on quarterly/annual.

### Other open

- **#72** White stuffer cards for fly packaging — dual-purpose: (a) inside customer packaging so black/dark patterns pop against white instead of vanishing into natural cardstock during unboxing; (b) backdrop for product photography of black/dark flies (supports the broader photo-improvement work).
  - Spec card size to match current packaging dimensions.
  - Source: Uline pre-cut cardstock (faster, ~$40 batch) vs office-supply cardstock + paper trimmer DIY (cheaper ~$20, more labor). Pick path.
  - Order initial batch ~200.
  - Estimated cost: $20–40 depending on path.

## Klaviyo email UTM convention

Standard for tagging all Klaviyo-sent email traffic so GA / GSC attribute by email type instead of bucketing everything as generic "klaviyo." Drop launch emails keep their existing spec under #52 — this covers everything else.

**Account-default** (set globally in Klaviyo → Account → Settings → UTM Tracking — applies to every link in every Klaviyo send unless overridden):

- `utm_source=klaviyo`
- `utm_medium=email`

**Per-campaign / per-flow overrides** (set at the campaign or flow-email level, override the account-default):

- **AC flow** (template `XU5bQs`): `utm_campaign=abandoned-cart`, `utm_content={template-or-variant-slug}`
- **PP flow** (template `RUBeHE`): `utm_campaign=post-purchase`, `utm_content={template-or-variant-slug}`
- **Crew weekly/monthly newsletter**: `utm_campaign=crew-newsletter`, `utm_content={yyyy-mm-dd}`
- **Ad-hoc blasts**: `utm_campaign={specific-slug}`, `utm_content={variant-or-asset}`
- **Drop launch emails**: see existing spec under #52 — `utm_campaign=drop-launch` already documented; don't change.

**Action items:**

1. Set the account-default in Klaviyo → Account → Settings → UTM Tracking — one-time, ~30 seconds.
2. Update AC flow + PP flow email nodes to use the campaign values above (Klaviyo flow email → Tracking Options).
3. Going forward, every campaign/flow gets a `utm_campaign` at creation time, no exceptions.

## Backlog (paused, not active)

- **#13** Avidmax wholesale walk-in prep — Nick contact.
- **#14** Umpqua signature pattern submission — blocked on Matt Winkler input.
- **#15** Local fly shop wholesale — South Park / North Park / Estes / Carter Lake corridors.
- **#45** The Drop — Phase 1 landing + waitlist validation (Square Subscriptions path, premium tier above free Crew newsletter).
  - `/the-drop.html` shipped 2026-05-12 with Klaviyo client-subscription wiring (list `SUpRkF`, custom_source `"The Drop Waitlist"`, profile property `drop_founding_interest`). Apps Script POST gated behind `ENABLE_DROP_APPSSCRIPT` flag in `the-drop.html` — flip to `true` after #47 ships.
  - **GATE to #46:** 25+ waitlist signups. If 4 weeks pass without 25 (≈ 2026-06-09), rethink positioning/pricing rather than lowering the gate.
  - **Locked decisions (2026-05-12):** founding $20/mo cap 10 lifetime / standard $35/mo / member discount 20% (code `DROP`) / bi-weekly Wed 7:00–8:30 PM MT / member chat tentatively Facebook (was Discord) / video platform TBD (Zoom or Meet) / ethics framing = spawn TIMING + closures + conditions, NOT locations.
- **#52** 4-week Drop sprint — 25 waitlist signups → opens Founding 10. 12 steps in 4 stages, work sequentially, ask before jumping. **Step 0 BLOCKER** was Klaviyo sender-domain auth (`send.trouttricks.com` zone — see #49); #49 closed 2026-05-16, so recheck whether Step 0 is now unblocked before Stage 1 ships.
  - **Stage 1 — Traffic:** (1) Klaviyo blast to 25 Crew subs, two variants drafted (Founding-10 urgency / Mastermind story); (2) IG / TikTok organic + paid Meta ads + cross-promo outreach + email follow-up flow (FB-group posts killed 2026-05-13); (3) DM script for top 10 engaged fans.
  - **Stage 2 — Conversion:** (4) Homepage above-the-fold rewrite to surface The Drop in first viewport mobile without killing the patterns tier; (5) /the-drop CRO audit — 5 specific improvements (urgency, FAQ accordion, mobile sticky CTA, simpler form); (6) Dead-click diagnosis via Clarity recordings.
  - **Stage 3 — SEO foundation:** (7) Fix duplicate URLs — `<link rel="canonical">` on every page, 301 the non-canonical; (8) Schema.org JSON-LD; (9) Chironomid Cheat Sheet PDF lead magnet (email-gated via new Klaviyo flow).
  - **Stage 4 — Round 2 (next 30 days):** (10) Round 2 sticker campaign with UGC angle; (11) 3 SEO blog posts engineered to internal-link to /the-drop; (12) Klaviyo retargeting flow — anyone who hits /the-drop and doesn't submit within 48 hrs gets follow-up.
  - **UTM convention (lock):** email `?utm_source=klaviyo&utm_medium=email&utm_campaign=drop-launch&utm_content={asset}` / DMs `?utm_source=dm&utm_medium=personal&utm_campaign=drop-launch&utm_content=personal-outreach` / IG organic `?utm_source=instagram&utm_medium=organic-social&utm_campaign=drop-launch&utm_content={post-type}-{angle}` / TikTok organic `?utm_source=tiktok&utm_medium=organic-social&utm_campaign=drop-launch&utm_content={angle}` / paid Meta `?utm_source=meta&utm_medium=paid-social&utm_campaign=drop-launch&utm_content={ad-creative}`.
  - **Voice rules:** real-angler first-person, no corporate slop ("premium quality"/"industry-leading"/"experience the difference" banned). "Tier" not "shop," "patterns" not "products," "tying bench" not "workshop." Spawn timing yes, spawn locations no.
- **Square Online merch store + Printful integration** — future scope, not started.

**Sub-tasks held until a parent ships:**

- **#21–24** Free Chironomid Cheat Sheet PDF + `/free-guide` landing page + homepage popup copy + reusable lead-magnet CTA block — release when #13 + #14 wrap.
- **#46** The Drop — Square Subscriptions setup + soft launch — release when #45 hits 25+ waitlist signups. Create founding ($20/mo, no end) + standard ($35/mo, no end) plans (test with $1/mo plan first); replace waitlist form with two CTAs ("Claim founding spot — $20" / "Join standard — $35") + manual spot counter ("X of 10 founding spots remaining"); create Square discount code `DROP` 20% off, no expiry, members-only.
- **#47** The Drop — Apps Script handler updates — release when #45 hits 25+ signups. Branch on `submission_type === 'drop_waitlist'` → "Drop Waitlist" tab; new "Drop Members" tab (timestamp_joined, name, email, plan_type, square_subscription_id, status, zoom_added, discord_added, founding_lock_date, last_status_change, notes); handle Square webhook events `subscription.created` / `subscription.canceled` / `invoice.payment_failed`. After deploy, flip `ENABLE_DROP_APPSSCRIPT = true` in `the-drop.html`.
- **#48** The Drop — member onboarding workflow + chat platform setup — release when #45 hits 25+ signups. Chat: FB private group (current lean) vs Discord fallback. Video: Zoom vs Meet. Recurring bi-weekly Wed 7 PM MT with cloud recording → member-only Drive folder. Welcome email template (next call date, archive link, discount code `DROP`, group invite, expectations). Cancellation workflow (remove from recurring invite + group).
- **#54** Wire cart to send structured address fields (`address_line1` / `city` / `state` / `zip`) and `acquisition_source` (e.g. `document.referrer`) so the new Code.gs columns auto-populate from real ship orders — release with next cart deploy.

## Closed (running log)

- **SSL cert apex-domain fix** — *closed 2026-05-24*. Background: Let's Encrypt cert was only valid for `www.trouttricks.com`, so anyone hitting the apex `trouttricks.com` over HTTPS got a browser cert warning. Two pieces shipped to land it:
  - **(a) CNAME flip to apex** (`f623d5f`): root `CNAME` file changed from `www.trouttricks.com` to `trouttricks.com`. GitHub Pages re-reads `CNAME` on every push; once apex is the canonical custom domain, Pages re-submits a Let's Encrypt cert request that covers BOTH apex and the auto-added `www.` SAN. DNS pre-verified — four `A` records at `@` already pointing to GitHub Pages IPs (`185.199.108-111.153`) on Namecheap, plus `www` CNAME — so the GH Pages DNS check passed instantly.
  - **(b) Namecheap URL Redirect Record deletion — the smoking gun**. Cert provisioning hung at "DNS check in progress" past the expected ~5–15 min window. Root cause: a stale **URL Redirect Record** in Namecheap's DNS panel (left over from an old `apex → www` redirect setup before GH Pages was wired) was intercepting Let's Encrypt's HTTP-01 challenge to `http://trouttricks.com/.well-known/acme-challenge/...` and 301'ing it to www, breaking the proof-of-control loop. Deleted the URL Redirect Record in Namecheap → cert provisioning unstuck within minutes → "Enforce HTTPS" checkbox went live and was ticked. Lesson: when GH Pages cert provisioning hangs despite correct A records, check Namecheap's URL Redirect Records (separate panel from Host Records) — they take precedence over A records for HTTP traffic and silently break ACME challenges.
  - **Verified live:** `https://trouttricks.com` loads with valid padlock, cert Subject Alternative Names list BOTH `trouttricks.com` and `www.trouttricks.com`, `http://trouttricks.com` 301s to HTTPS, www continues to work and auto-redirect to apex. Browser cert warning on apex is gone.
- **#55** Daily reconciliation script — *closed 2026-05-22 (live-verified)*. New `apps-script/daily-reconciliation.gs` file pasted into the Apps Script editor alongside `Code.gs`. Pulls Square `/v2/payments` over a 48h window (24h alert window — wider join-side fetch so sheet rows at the 24h edge don't false-alarm), diffs against `payment_id` extracted from the Details column (col F) of every Orders/Fulfillment row, emails `thomas@trouttricks.com` on either direction: (1) Square payment with no matching sheet row — PRIMARY alarm, indicates webhook didn't fire; (2) sheet row with payment_id Square doesn't know — usually test/manual drift. Top-level try/catch sends a "FAILED" email on any unhandled exception so silent breakage can't hide. Setup: `SQUARE_ACCESS_TOKEN` (Square production access token) added to Script Properties; `installDailyReconciliationTrigger()` ran cleanly (6:00 MT daily); test run of `dailyReconciliation()` clean — 0 Square payments in the 48h window, 0 mismatches, no email. Tail of fix: Apps Script V8 scopes `const` per-file, so `ORDERS_SHEET`/`COL_DETAILS` (defined in Code.gs) were not visible from the recon file — re-declared them at the top of `daily-reconciliation.gs` (keep in sync with `Code.gs:1` / `Code.gs:12` if either ever moves). Addresses the fire-and-forget root cause behind Carly Beard's 2026-05-16 missing-address incident (#57): if a webhook ever silently fails to land, Thomas now finds out within 24 hours via email instead of when the customer DMs about an order he never saw.
- **#18** Klaviyo automation — abandoned cart + post-purchase flows — *closed 2026-05-21 (tt11)*. Fully shipped end-to-end:
  - **Klaviyo onsite tracker installed sitewide** (`08b9b76`) — async snippet (`static.klaviyo.com/onsite/js/klaviyo.js?company_id=RyxZFj`) injected into `<head>` of every content HTML page (12 cart-bearing root + 2 non-cart root + 22 blog posts + 2 hand-maintained category pages + 30 per-fly via generator template). `window._learnq` now defined on every content page; also enables Active on Site + future Viewed Product / Search events sitewide. Skipped intentionally: 0-byte Google verification stub and `flies/scuds.html` redirect stub.
  - **Cart event tracking** via `js/klaviyo-cart.js` (`54e363c`) — shared payload builder resolves fly metadata via `catalog.js` + `FLY_IMAGES`, guarded on `window._learnq`. `Added to Cart` fires after `updateCartUI()` in `addToCart()` across all 12 cart pages + 30 per-fly `quickAdd()`; `Started Checkout` fires in `openCart()` when `cart.length >= 1`.
  - **`window.flies` → `flies` bug fixed** (`0a9faee`) — catalog declares `flies` as a script-level `const`, so `window.flies` was undefined and `buildPayload()` was returning null (events silently no-op'ing). Helper now references `flies` directly with a `typeof flies === 'undefined'` guard so a failed `catalog.js` load still doesn't throw.
  - **Inline cart email capture for cold-visitor identification** (`8c73f98`) — optional `#cartEmailInput` field above the Checkout button on all 12 cart-bearing pages (anchor varied: index uses "CHECKOUT" text, the other 11 use "Proceed to Checkout"). On `blur`/`change` validates with `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`, saves to `localStorage.tt_customer_email`, pushes `_learnq.push(['identify', { '$email', '$source': 'cart-panel' }])`. Persists across sessions; silent idempotent re-identify on every page load. Invalid/empty input is a silent no-op (low-pressure UX); never blocks checkout. Per-fly pages intentionally skipped (Quick Add only, no full cart panel).
  - **AC flow LIVE**: `Started Checkout` → 1 hr delay → template `XU5bQs`. Profile Filter: `Placed Order zero times since starting this flow`.
  - **PP flow LIVE**: `Placed Order` → 14 day delay → template `RUBeHE`.
- **#38** Free sticker with fly order — *closed 2026-05-20 (retroactively closed — was already shipped)*. Gift-with-purchase mechanic lives in `js/promo-tiers.js:46-51`: any cart containing at least one fly pack auto-adds a free "Surprise Sticker" line at $0 via `getPromoTiers(cart)`. Renders in the cart panel (`index.html:4516-4519`), the order summary (`index.html:4600-4601`), the email line (`index.html:4633`), and the Worker checkout payload (`index.html:4687-4691`) so Thomas knows to include one in every box. Plus a sitewide promo bar above the header (`index.html:2936`) announces the freebie. Tier stacks with the "5 packs → 1 free pack" buy-5-get-1 mechanic in the same file.
- **#40** Stillwater money-keyword SEO sprint (Antero / Spinney / Eleven Mile / Delaney+Lake John) — *closed 2026-05-20*. Five-wave keyword-domination sprint across the four lake posts plus balanced-leech and the two Antero news posts. **Wave 1** (`b86b614`, 2026-05-11): surgical title/H1 rewrites + leech link fixes. **Wave 2A** (closed 2026-05-20, no commit): retroactively confirmed complete — `#33b`'s per-fly pages migration on 2026-05-12 had already replaced `../index.html#catalog` anchors with `/flies/<slug>.html` direct links across all six target posts. **Wave 2B** (`6e975fb`/`0fe3cfc`/`7c952b2`, 2026-05-20, 5 days ahead of GSC deadline): Antero post pivoted from generic "best chironomid patterns" to a drain-story / 5-species-mix narrative (277w → 1,236w); Spinney expanded 490w → 1,226w with new Indicator Setup / Wind Strategy / Common Mistakes H2s; Delaney expanded 520w → 1,313w with new Three Lakes / When to Fish / Indicator / Wind H2s. Both expanded posts repositioned around the post-Antero traffic shift. **Wave 2C** (`862bd59`, 2026-05-20): cross-linking across all four posts; new "Eleven Mile vs Spinney" comparison H2 in Eleven Mile (709w → 1,650w, replacing an awkward "Backlink-Worthy Fishery" SEO-meta section); both Antero news posts (closure-2026, replacement-playbook) gained a May 20 update callout funneling readers to the new drained-patterns post; balanced-leech post inline-linked to all three remaining lake posts. **Wave 3** (`ba48812` + `63afab8`, 2026-05-20): FAQPage + BreadcrumbList JSON-LD on all four lake posts (5 Q&A per post, JSON-validated); 11 inline pattern photos with lake-specific alt text using existing /images/fly\*.jpg + balanced-leech.jpg + mayers1.jpg. **Net delta:** ~3,500 words of new tactical content, 12 new schema blocks, 11 inline images, ~30 new cross-links — all shipped inside the GSC attribution window from Wave 1. **Follow-up:** Thomas corrected the legacy "rainbow, brown, lake trout" Delaney species claim to "rainbow and brown" (`7cc4c5b`, 2026-05-20) — lake trout aren't in the Delaneys. The dedicated `antero-reservoir-chironomid-patterns.html` post (now the drain story) will need updating as facts surface re: CPW salvage, drain status, and refill timeline.
- **#70** Klaviyo ↔ Master Customers reconciliation backfill — *closed 2026-05-20*. Added `backfillKlaviyoToMasterCustomers()` to `apps-script/Code.gs` (paginates Klaviyo list `SUpRkF` profiles, diffs against MC by email, appends missing with Source `'Klaviyo Backfill'` + Klaviyo `created` as timestamp). Idempotent — re-running is safe. Setup required a one-time Klaviyo Private API Key in Script Properties as `KLAVIYO_PRIVATE_API_KEY` (read:profiles + read:lists scopes). First run on 2026-05-20: fetched 30 Klaviyo profiles in 1 page, 22 already present, 8 new rows added. Going-forward integrity now fully maintained by #62's crew_signup wiring + this backfill function (kept in the codebase for ad-hoc re-runs).
- **#67** `LockService` around `handleSquarePaymentCompleted` dedup → write — *closed 2026-05-20*. Wraps everything from the details-column read through appendRow/setValue calls in `LockService.getDocumentLock().waitLock(10000)` with release in `finally`. On lock timeout: log + return `{success: false, error: 'lock_timeout'}`. Defensive fix for the suspected tt10 double-write (rows 15+16 both showed `z9iiVy01...` payment_id before manual delete). Also brought the repo mirror back in sync with the #64(c) editor-only rewrite (was still showing the old ghost-only path). Deployed via pencil ✏️ `doget alive 12` → New Version.
- **#65** Thread name + structured context through `submitEmailSignup` — *closed 2026-05-20*. `submitEmailSignup(email, source)` gained an optional third arg `extra` (`{firstName, lastName, phone, notes}`); fields flow through to the Apps Script POST as `customer_first_name`/`customer_last_name`/`customer_phone`/`notes`. Three pickup callers updated to thread DOM-collected name+phone: `checkoutWithSquare` (cart panel), `pickupCardCheckout` (Guides Choice card), `placeOrder` (legacy form). Newsletter callers (`popup_signup`/`footer_signup`) untouched — stay 2-arg, no behavior change. Apps Script `writeToMasterCustomers` gained a `'Pickup Order'` branch writing `'Pickup pre-checkout · LastName: X · Phone: Y'` into Notes (First Name col fills for free now that `customer_first_name` is sent). Deployed via pencil ✏️ `doget alive 12` → New Version. Mirror in `apps-script/Code.gs` matches live (paste hit a transient `} }` syntax error during deploy; corrected before publish).
- **#71** Square pickup checkout pre-fill UX — *closed 2026-05-20 (verified)*. After #64(e) removed `pre_populated_data` for pickup orders, the open question was whether Square would auto-populate buyer email/phone on its checkout page from the PICKUP fulfillment recipient the Worker attaches. Confirmed via TT-47792 live test on 2026-05-19: UX was clean, no regression.
- **#69** Phone column `+` prefix — *closed 2026-05-20 (won't-fix)*. Worker sends E.164 (`+13035551234`); Sheets evaluates the leading `+` as unary-plus and stores the value as the number `13035551234`. Turns out this is exactly the format marketing platforms want (Meta Custom Audiences, Klaviyo CSV, Google Ads phone match all expect digits-only). Reversible later if needed: ~5 min to patch Apps Script to write `"'+" + digits` for new rows, ~5 min to backfill existing rows with a `="+"&D2` helper column + paste-as-values. Underlying number is lossless (no scientific-notation precision loss), so future backfill has clean input.
- **#68** Kill legacy pre-write fetch from `pickupCardCheckout()` — *closed 2026-05-20*. Guide's Choice entry point still ran the cart-side Apps Script pre-write (`payment_method='square_pending'`) that #64 removed from `checkoutWithSquare()`, so any Guide's Choice pickup would re-pollute the to-tie queue with unpaid rows. Same patch shape as `f840bd9`: deleted the `fetch('https://script.google.com/macros/s/...')` block + payload assembly, kept `submitEmailSignup(email, 'Pickup Order')` for Klaviyo capture. Function dropped from 41 lines to 8 (firstName/lastName/phone/fullName no longer needed locally — `checkoutWithSquare` reads them itself at line 5200-5206). Comment block rewritten to document the tt10 architecture.
- **#66** Contact form submission notifications — *closed 2026-05-19* (verified in production). Added `MailApp.sendEmail()` block to `handleContactForm` in `apps-script/Code.gs:58` — fires every submission to `thomas@trouttricks.com` with `replyTo` set to the customer's email (Reply in Gmail goes straight to them). Subject `📬 Trout Tricks contact: <reason> — <firstName>`; plaintext body with timestamp + name/email/phone/reason/message. Wrapped in try/catch with `logWebhook` fallback to the Webhook Log tab so MailApp hiccups (quota, transient) don't block the sheet append. Refactored field reads into local consts (firstName/email/phone/reason/message/optInRaw) since they're used by both the sheet row and the email. Drops dependence on EmailJS client-side leg (`contact.html:2886`, template `template_7wna2ba`) which was the silent-failure mode behind the 2026-05-19 SEO spammer incident. Local mirror committed; live editor deployment + verification done out-of-session by Thomas.
- **#64** Kill cart-side pre-write + new architecture: only paid Square orders land in Master Tracker — *closed 2026-05-19*. Background: pickup-checkout refactor `15b8a01` introduced a cart-side Apps Script pre-write fetch (`payment_method='square_pending'`) that ran BEFORE Square redirect to capture bail customers. Worked but polluted Thomas's to-tie queue with unpaid rows. Five-piece refactor across three subsystems shipped in tt10:
  - **(a) Worker → forward `fulfillment_type` + pickup recipient** (`7701af4`): added PICKUP branch alongside existing SHIPMENT branch in `handleSquareWebhook`, reads `pickup_details.recipient.{display_name,email_address,phone_number}` for pickup fulfillments. Apps Script now has fulfillment context on every paid order.
  - **(b) Worker → forward `order_items`** (`19cab9b`): formats `order.line_items` into newline-separated string, skips the "Shipping" pseudo-line-item handleCheckout adds for ship orders. Fixes the silent "ghost rows lack items" bug ship orders had pre-tt10 (Thomas didn't realize ship workflow was also broken until flagged in this session).
  - **(c) Apps Script `handleSquarePaymentCompleted` ghost-path rewrite** (no commit — Apps Script editor only): full rewrite. Cart-originated orders (have `reference_id`) get clean rows — parsed first/last name, Fulfillment column populated, items in Details, blank Notes/Tracking, source `'Order Paid'` in Master Customers. Truly external Square sales (no `reference_id` — POS, Invoice) keep `⚠️ EXTERNAL ORDER` warning + `(external)` in notes + `'Order Paid (Ghost)'` source. Added test function `testCartPickup_v60` for in-editor regression checks. Deployed via pencil → New Version.
  - **(d) `index.html` → kill cart pre-write fetch** (`f840bd9`): surgical removal of the Apps Script `fetch` from `checkoutWithSquare()`'s pickup block. Klaviyo `submitEmailSignup` preserved (still captures bail customers for marketing). 30 lines deleted / 5 lines explanatory comment added. *Note: legacy `pickupCardCheckout()` (Guide's Choice entry point) still has the old pre-write fetch — see #68.*
  - **(e) Worker → pre-attach PICKUP fulfillment with recipient** (`7b71b7a` + `1ef69ea`): Square doesn't auto-create PICKUP fulfillments because it never asks the buyer for shipping info, so without this the webhook handler couldn't find recipient data for pickup orders. Worker attaches `type:"PICKUP"`, `state:"PROPOSED"`, `pickup_details.recipient` (name/email/phone from cart) and `pickup_at` 7 days out (placeholder). Initial deploy hit Square API rejection `CONFLICTING_PARAMETERS: "Only one of [fulfillment, buyer_email] fields should be set."` — fixed in `1ef69ea` by gating `pre_populated_data` on SHIP orders only.
  - **Live-verified end-to-end with TT-47792** (pickup, $5.00, 2026-05-19 15:34): row landed with `Thomas / Frank / 13039178278 / items list / Fulfillment: LOCAL PICKUP / Local pickup col G / Ordered / blank notes`. Master Customers landed as `Order Paid · Thomas · Order #TT-47792 · Local pickup · $5.00 · paid`. Bail-out test (no payment): zero rows to Orders/Fulfillment, Klaviyo profile captured. Square's webhook retry queue also flushed several backlogged old test payments through during the session — Thomas deleted those manually. Commits: `7701af4`, `19cab9b`, `f840bd9`, `7b71b7a`, `1ef69ea`.
- **#63** Klaviyo sender footer — *closed 2026-05-19*. Stale Gmail in Klaviyo Settings → Email → Sender information was being used as the footer "From" address despite #49's `thomas@trouttricks.com` swap. Thomas located + corrected the stale entry in the Klaviyo admin UI. No code changes.
- **#61** Pre-populate Square Hosted Checkout customer info — *closed 2026-05-19* (`ea9a9ac`). Worker `handleCheckout` now adds `pre_populated_data` (`buyer_email`, `buyer_phone_number`) to the Payment Links request when the cart forwards them; phone normalized to E.164. **Scope adjusted in #64 (e):** SHIP orders only — pickup orders rely on `fulfillment.pickup_details.recipient` to populate Square's checkout page, since setting BOTH conflicts with Square's API (`CONFLICTING_PARAMETERS`). First/last name skipped from `pre_populated_data` per Square's docs (not reliably honored).
- **#62** Mirror Klaviyo Crew newsletter signups to Master Customers — *closed 2026-05-19*. Background: footer + popup email signups on `index.html` were Klaviyo-only (since the Zapier deprecation), so newsletter subs never landed in the CRM — confirmed when rdknoll@yahoo.com and coolpapatony@gmail.com hit Klaviyo on 2026-05-18 without writing a Master Customers row. Three pieces shipped: (a) new `handleCrewSignup(body)` Apps Script handler, routed from `doPost` on `submission_type === 'crew_signup'`, writes Master Customers with Source = `body.acquisition_source` (e.g. `'Website Popup'` / `'Website Footer'`) and Notes = `'Newsletter signup'`; (b) `submitEmailSignup()` in `index.html` now fires a fire-and-forget Apps Script POST (text/plain content-type to skip CORS preflight) alongside the existing Klaviyo subscription POST; (c) `writeToMasterCustomers` branch updated to recognize the specific entry-point source values. Live-verified via popup + footer test signups; both landed in Master Customers with correct Source column values. Commits: `b9f2ad8` (handler) / `4b1cf19` (cart) / `7d4bea7` (Source label refinement — entry point in Source column instead of generic 'Crew'). Historical signups (rdknoll, coolpapatony) backfilled manually by Thomas; future signups auto-flow.
- **#60** Reconcile Apps Script writes with real sheet structure — *closed 2026-05-19*. All three original sub-issues plus a fourth (matched-row idempotency) shipped and live-verified with TT-48875 pickup test:
  - **(a) Status column taxonomy** — collapsed to a single workflow value: handleOrder + handleSquarePaymentCompleted both write `'Ordered'`. Payment confirmation no longer touches the Status column — the Square Payment block (payment_id, receipt URL, completed timestamp) is appended to the Details cell (col F) instead. Daily reconciliation (#55) will distinguish paid from abandoned later.
  - **(b) Address column drift** — Apps Script now uses column-position constants at top of `Code.gs` (`COL_STREET=12` ... `COL_ZIP=15`) and writes to L-O on both matched-row and ghost paths. Header-name lookup deferred (constants are good enough for now; a header shuffle would surface immediately on test).
  - **(c) Ghost-path idempotency** — `handleSquarePaymentCompleted` ghost branch checks `payment_id` against existing Details cells before appending a new ghost row; suppresses duplicate ghost append when Square fires `payment.updated` 5–7 times per payment.
  - **(d) Matched-row idempotency** (surfaced 2026-05-19 once #58 fallback unlocked matched-row updates) — same `payment_id` check at top of the matched-row branch; suppresses duplicate Square Payment block append AND duplicate Master Customers `'Order Paid'` row. Verified live: TT-48875 webhook fired 5×, only one payment block written, only one Master Customers row.
  - Commits: `f409e6f` (prep — status taxonomy, column constants, ghost dedup on feature/60 branch) / `01a3605` (matched-row dedup) / `da3d7ab` (merge to main).
- **#58** Cart → Worker → Square `reference_id` pass-through — *closed 2026-05-19 (live-verified)*. Cart-side (`index.html` `checkoutWithSquare`) now generates `order_number` via `generateOrderNumber()` (added guard for ship path; pickup path already had one via `goCheckoutLegacy`) and sends it in the Worker POST. Worker (`workers/worker.js`) reads `body.order_number` and attaches as `order.reference_id` on the Square Payment Link create call (40-char cap, field omitted when missing for backward compat). Worker webhook handler initially assumed `payment.reference_id` would echo what was set on the Order — turned out Square's Payment object does NOT inherit reference_id from its parent Order on `payment.updated` (always null), so a follow-up Worker patch (`96b6513`, 2026-05-19) reads `orderData.order.reference_id` as a fallback when `payment.reference_id` is null. Without that fallback, every cart order silently fell through to the ghost path. Merged via rebase + FF to main (`b3e0605`), GH Pages built at `2026-05-18T05:24:31Z`. **Live-verified 2026-05-19** with TT-48875 (pickup, $4.50): webhook returned `reference_id: "TT-48875"`, matched-row update fired, no ghost row, payment block appended to col F, Master Customers got single 'Order Paid' row.
- **#57** Cloudflare Worker + Apps Script Square shipping-address pull — *closed 2026-05-17*. Root cause behind Carly Beard's 2026-05-16 missing address: `handleSquareWebhook` never fetched the linked order, and `handleSquarePaymentCompleted` ignored address fields. Worker now `GET /v2/orders/{payment.order_id}` → extracts recipient + address from SHIPMENT fulfillment → forwards `customer_name`/`customer_phone`/`address_line1`/`city`/`state`/`zip` in normalized payload. Apps Script writes K-N (address) + D (phone) + A (email, preserve-existing rule) on matched-row path; ghost-order path appends with full address. Worker mirror lives at `workers/worker.js`; Apps Script `Code.gs` mirror still deferred. Deployed end-to-end and verified via test ghost row. **#57b:** payment_id-fallback guard added — Apps Script's original `if (!data.reference_id) skip` would have rejected real ship orders (cart/Worker doesn't set `reference_id` on the Square Payment Link), so matching now falls back to `payment_id` when `reference_id` is null. **Sheets cleanup (tt8 Move 3, 2026-05-17):** Carly's row 11 backfilled K-N + H → Paid from Square dashboard; Frank's row 10 H → Paid; 2026-05-17 sticker test transaction refunded and excluded from order/reporting totals.
- **#49** Klaviyo sender swap to `thomas@trouttricks.com` — *closed 2026-05-16*
- **#17** Phase 3 Square post-payment webhook → Apps Script auto-flip status — *closed 2026-05-16*. Deployed via New Version on `doget alive 12` (Cloudflare `APPS_SCRIPT_WEBHOOK_URL` secret preserved). Square Send Test Event returns 200 OK end-to-end. Three batched Code.gs edits landed together (no separate task IDs — prep work for Carly's investigation): (a) `writeToMasterCustomers` Acquisition Source column reading `body.acquisition_source || body.referrer`; (b) `handleOrder` structured address parsing (street/city/state/zip), appendRow extended to 14 elements; (c) `handleSquarePaymentCompleted` ghost-order appendRow extended to 14 elements with 4 trailing empty strings. **Bonus:** `logWebhook` function added + called from top of `doPost` — auto-creates a "Webhook Log" tab and records every incoming webhook (timestamp, source, event type, customer info, raw payload). Verified live with a Square test event.
- **Media Kit page** — built `media-kit.html` from the design brief (`assets/media-kit-design-prompt.md`, committed 6a495a3). Single-page web kit covering cover/about+founder/4 pattern categories/The Drop callout/stats+verified reviews/partners grid/boilerplate copy (25/50/100w)/press contact card/back cover. Reuses site header + footer; matches `:root` palette and Bebas/Barlow type. Footer Quick Links updated on all 14 pages (+ sitemap.xml entry, priority 0.5). PDF deliverable not built — page references "email for PDF" in lieu of broken download link. — *closed 2026-05-14*
- **#33b** Per-fly static pages — generated 20 landing pages at `/flies/<slug>.html` (12 fly patterns + 6 stickers + 2 bundles) via `scripts/generate-fly-pages.py`. Each carries unique `<title>` / meta description / OpenGraph / Twitter cards / Product JSON-LD / BreadcrumbList JSON-LD. Sitemap.xml updated with all 20 entries. Homepage catalog cards now show a "View Details →" link below price (per option-a decision — clicking the photo still expands the lightbox). The in-page share button now shares the per-fly URL (`/flies/balanced-leech.html`) instead of the homepage hash, so iMessage/FB/X finally render a real preview card with the fly name + price + image. Re-run the generator after adding any new fly. — *closed 2026-05-12*
- **#33a** PATH A refactor — extracted `flies[]` + `FLY_IMAGES` + `slugifyFlyName` from 12 separate HTML files into a single canonical `/js/catalog.js`. fish.html had drifted (missing stickers, empty FLY_IMAGES); now unified. Cart functions intentionally left duplicated (lower risk). Prereq for #33b. — *closed 2026-05-12*
- **#39** GBP video walkthrough verification — *in process 2026-05-12* (submitted for Google review; tracking moved off queue)
- **#44** Fish gallery WebP variants — converted 111 v2fish JPGs to WebP @ q=80 via cwebp; wrapped each `<img>` in `<picture>` with WebP source + JPG fallback. WebP-capable browsers (95%+ market) get ~5.7 MB total instead of 12 MB; legacy browsers fall back to JPG. Combined with #42 (resize) + #43 (pagination), initial gallery load is ~30 imgs × ~50 KB WebP avg = ~1.5 MB. Down from original 46 MB unpaginated. — *closed 2026-05-12*
- **#43** Fish gallery pagination — initial 30 imgs render; rest carry `hidden` attr until "Show more" button reveals next batch of 30. Combined with lazy-loading, initial gallery page-weight drops to just the visible 30 imgs (~3 MB) instead of all 111 (~12 MB) — *closed 2026-05-12*
- **#42** Fish gallery — resize all 111 v2fish JPGs from source res (1200–1600px) to 600px wide via sips; updated width/height attrs in fish.html accordingly — *closed 2026-05-12* (46 MB → 12 MB total, ~4× lighter)
- **#41** Sticker campaign Apps Script field-name fix — *closed 2026-05-11* (free-sticker.html: `address_line_1` / `address_line_2` → `address_line1` / `address_line2` to match what `handleStickerCampaign()` reads. Round 1 lost addresses due to underscore-before-digit mismatch. **Manual TODO for Thomas:** retype "Address Line 1" header into column F of the Sticker Campaign tab in the Master sheet — header was missing on the sheet side, separate from the payload bug.)
- **#12** GBP photos + video upload — *closed 2026-05-11* (photos uploaded; more added incrementally as available)
